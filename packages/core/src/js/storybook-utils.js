import * as Storybook from '@storybook/html';
import * as Knobs from '@storybook/addon-knobs';
import { forModule } from 'storybook-addon-angularjs';
import centered from '@storybook/addon-centered/html';

export {
  Storybook,
  Knobs,
  forModule,
  centered,
};
