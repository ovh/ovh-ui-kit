import Button from '@ovh-ux/ui-kit.button';

import './index.less';

const moduleName = 'button-stories';
const moduleStyle = 'sb-button-stories';

angular.module(moduleName, [Button]);

export {
  moduleName,
  moduleStyle,
};
