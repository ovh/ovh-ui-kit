const styles = {
  wrapper: {
    position: 'fixed',
    top: 0,
    left: 0,
    bottom: 0,
    right: 0,

    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',

    overflow: 'auto',
  },
  inner: {
    flex: 1,
    textAlign: 'center',
    padding: '25px',
  },
};

export default styles;
