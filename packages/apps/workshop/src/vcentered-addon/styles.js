const styles = {
  wrapper: {
    position: 'fixed',
    top: 0,
    left: 0,
    bottom: 0,
    right: 0,

    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',

    overflow: 'auto',
  },
  centered: {
    maxHeight: '100%',
    flex: 0,
  },
  inner: {
    textAlign: 'left',
    padding: '25px',
  },
};

export default styles;
